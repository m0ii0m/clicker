services:
  backend:
    image: clicker-backend:latest
    container_name: clicker-backend
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      REDIS_HOST: redis
    depends_on:
      - redis
  frontend:
    image: clicker-frontend:latest
    container_name: clicker-frontend
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
  redis:
    image: redis:alpine
    container_name: clicker-redis

# docker-compose up -d --build
# docker-compose down
